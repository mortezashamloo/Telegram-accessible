--- a/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/DialogsActivity.java
@@ -13765,6 +13765,11 @@
                 presentFragment(new SettingsActivity());
             });
         }
+        // Accessibility: new item for the a11y settings screen, always
+        // reachable regardless of the showCallsTab condition above.
+        io.add(R.drawable.msg_settings_old, "Accessible Settings", () -> {
+            presentFragment(new org.telegram.ui.AccessibleSettingsActivity());
+        });
 
         if (proxyMenuSubItem != null) {
             proxyMenuSubItem.subtextView.setTextColor(getThemedColor(Theme.key_groupcreate_sectionText));
